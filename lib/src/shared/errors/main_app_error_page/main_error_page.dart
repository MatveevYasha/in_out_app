import 'package:flutter/material.dart';

class MainErrorPage extends StatelessWidget {
  const MainErrorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Произошла ошибка');
  }
}
